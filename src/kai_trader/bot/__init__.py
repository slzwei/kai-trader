"""Telegram bot package.

The bot is the primary human interface to Kai Trader. In Phase 1 it only
supports read-only commands with mocked trading data; later phases wire in
real portfolio and position queries plus write commands (pause, resume,
kill-switch).
"""
